{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary mt-3">
        <i class="fas fa-save me-2"></i>{{ button_label|default('Créer') }}
    </button>
{{ form_end(form) }}
